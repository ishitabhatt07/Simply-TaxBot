# 🇮🇳 TaxBot: Thorough ITR Filing Help Bot

This is a RAG (Retrieval-Augmented Generation) chatbot built with LlamaIndex, Streamlit, and Ollama to answer questions about Indian tax filing.

The bot uses a local vector database (ChromaDB) to store and retrieve information from a set of official ITR guides and FAQs.

## 🚀 How to Run

1.  **Prerequisites:** Have **Ollama** installed and running with `ollama run llama3`.
2.  **Install:** Run `pip install -r requirements.txt`.
3.  **Run:** Run `streamlit run app.py`.